[phases.setup]
nixPkgs = ["python311", "nodejs_20"]

[phases.install]
cmds = [
  "pip install -r requirements.txt",
  "npm install",
  "npm run build"
]

[phases.build]
cmds = ["mkdir -p data"]

[start]
cmd = "python server.py"
