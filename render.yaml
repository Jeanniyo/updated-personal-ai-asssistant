services:
  - type: web
    name: ai-assistant
    env: python
    buildCommand: pip install -r requirements.txt && npm install && npm run build
    startCommand: python server.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
